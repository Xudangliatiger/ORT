# Third-party notices

The transformer and randomized-order generator implementation is adapted from:

- `bytedance/1d-tokenizer` (Apache-2.0), including the RAR implementation.
- `FoundationVision/LlamaGen` (MIT).

The released configuration targets AliTok tokens. AliTok weights and decoder code are
not redistributed here; obtain them from the official `ali-vilab/alitok` repository.

This anonymous package contains no pretrained model weights or training data.
